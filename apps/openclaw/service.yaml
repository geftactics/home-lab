apiVersion: v1
kind: Service
metadata:
  name: openclaw
  namespace: default
spec:
  selector:
    app: openclaw
  ports:
    - name: gateway
      protocol: TCP
      port: 443
      targetPort: 18789
  type: ClusterIP
